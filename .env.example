# Supabase — create at supabase.com, run schema.sql, then paste keys here
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# SHA-256 hash of the admin PIN. Generate with:
#   echo -n "YOUR_PIN" | shasum -a 256
# Defaults to PIN "1234" when unset.
VITE_ADMIN_PIN_HASH=
